// Assign a new and additional task to an existing resource.
// e.g. SmartPhone, StudentChair, WindshieldDefrosterAntenna, DumbbellWaterBottle
function getVirtualProductsByTaskUnification(closedWorldComponents) {

    var virtualProducts = [];
        
    //TODO: Allocate an internal task to an external component.

    //TODO: Allocate an external task to an internal component.

    //TODO: Assign a task performed by an internal component to another internal component.
    
    return virtualProducts;
}