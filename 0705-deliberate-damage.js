// Deliberate damage.
// e.g. Navigation, Poseidon, Pit, AncientMonuments
function getVirtualProductsByDeliberateDamage(closedWorldComponents, systemVariables) {

    var virtualProducts = [];
        
    //TODO: Try ruin something on purpose, add artificial constraints, and make things worse.
    
    return virtualProducts;
}