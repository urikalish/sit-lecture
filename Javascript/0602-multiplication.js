// Add to a product components of the same type as an existing component.
// e.g. Nail
function getVirtualProductsByMultiplication(closedWorldComponents) {

    var c, virtualProducts = [];
    
    for (c = 0; c < closedWorldComponents.length; c++) {

        //TODO: Multiply a component and leave it as-is.

        //TODO: Multiply a component but change it in some way.
    }

    return virtualProducts;
}