// Remove an essential component from a product.
// e.g. SoupPowder, HangingBabyChair, TV, LaundryFluid, GrindingDisc
function getVirtualProductsBySubtraction(closedWorldComponents) {

    var c, virtualProducts = [];
    
    for (c = 0; c < closedWorldComponents.length; c++) {

        //TODO: Remove a component and use the product as-is.

        //TODO: Remove a component and replace it by another component from the closed-world.

        //TODO: Remove a component and replace it by an external component.
    }

    return virtualProducts;
}